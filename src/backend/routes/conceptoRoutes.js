// import { Router } from 'express';
// import {
//     getConceptos, createConcepto, getConceptoById, updateConcepto, deleteConcepto
// } from '../controllers/conceptoController.js';
// import { validateSchema } from "../middlewares/validateSchema.js";
// import conceptoValidator from "../schemas/conceptoValidator.js"

// const router = Router();
// const conceptoSchema = conceptoValidator;

// //Get all conceptos
// router.get('/conceptos', getConceptos);

// //Create nuevo concepto
// router.post('/conceptos', createConcepto);

// //Get concepto por id
// router.get('/conceptos/:id', getConceptoById);

// //Update concepto
// router.put('/conceptos/:id', validateSchema(conceptoSchema), updateConcepto);

// //Delete concepto
// router.delete('/conceptos/:id', deleteConcepto);

// export default router;