// import { Router } from 'express';
// import {
//     getRemitos, createRemito, getRemitoById, updateRemito, deleteRemito
// } from '../controllers/remitoController.js';
// import { validateSchema } from "../middlewares/validateSchema.js";
// import remitoValidator from "../schemas/remitoValidator.js"

// const router = Router();
// const remitoSchema = remitoValidator;

// //Get all remitos
// router.get('/remitos', getRemitos);

// //Create nuevo remito
// router.post('/remitos', createRemito);

// //Get remito por id
// router.get('/remitos/:id', getRemitoById);

// //Update remito
// router.put('/remitos/:id', validateSchema(remitoSchema), updateRemito);

// //Delete remito
// router.delete('/remitos/:id', deleteRemito);

// export default router;